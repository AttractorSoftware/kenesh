{% extends 'AdminBundle::admin-base.html.twig' %}

{% block container -%}
    <h1>Deputy edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_deputy') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
